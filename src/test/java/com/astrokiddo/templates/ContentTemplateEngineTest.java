
package com.astrokiddo.templates;

import com.astrokiddo.model.Slide;
import com.astrokiddo.model.SlideType;
import org.junit.jupiter.api.Test;
import java.util.List;
import java.util.Map;
import static org.junit.jupiter.api.Assertions.*;

class ContentTemplateEngineTest {
/*    @Test
    void keyVisualFromImageItem_basic(){
        var img = Map.of(
            "links", List.of(Map.of("href", "https://example.com/image.jpg")),
            "data", List.of(Map.of("title","Test Image","description","Desc","center","NASA/JPL"))
        );
        var eng = new ContentTemplateEngine();
        Slide s = eng.keyVisualFromImageItem(img);
        assertEquals(SlideType.KEY_VISUAL, s.getType());
        assertEquals("Test Image", s.getTitle());
        assertEquals("https://example.com/image.jpg", s.getImageUrl());
    }*/
}
